{
  "name": "hybrid-maps-platform",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm --filter @hybrid/hgk test && pnpm --filter @hybrid/maps-spatial-engine test && pnpm --filter @hybrid/maps-core test && pnpm --filter @hybrid/maps-runtime test",
    "typecheck": "pnpm -r build"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^5.0.0"
  }
}
