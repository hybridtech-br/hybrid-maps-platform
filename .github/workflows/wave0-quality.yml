name: Wave 0 Quality Gates

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9.15.0
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build and typecheck Wave 0 workspace
        run: pnpm --filter '!@hybrid/hgk' -r build

      - name: Run characterization and existing package tests
        run: pnpm --filter '!@hybrid/hgk' -r test

      - name: Verify API contract fixtures explicitly
        run: pnpm --filter @hybrid/maps-api-contract-tests test
